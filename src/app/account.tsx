import { AccountScreen } from "@/features/account/AccountScreen";

/** Thin route (docs/01 rule 1): the Account tab is features/account's screen. */
export default function AccountRoute() {
  return <AccountScreen />;
}
