import { RecordScreen } from "@/features/recording/RecordScreen";

/** Record tab — thin route; everything lives in features/recording. */
export default function RecordRoute() {
  return <RecordScreen />;
}
