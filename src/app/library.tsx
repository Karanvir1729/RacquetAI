import { LibraryScreen } from "@/features/recording/LibraryScreen";

/** Library tab — thin route; everything lives in features/recording. */
export default function LibraryRoute() {
  return <LibraryScreen />;
}
